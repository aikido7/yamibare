# 方向宣言: ヤミバレ

> この文書が以後の全UI作業の正典。実装中にここから外れたくなったら、**先にこの文書を更新してから**外れる。

## 診断（2026-08-09 実施・計数結果）

| 項目 | 現状 | 判定 |
|---|---|---|
| 使用色数 | **30色** | ⚠️ 危険信号（10色超）。似た暗色が10種類乱立 |
| フォントサイズ | **18種**（10/10.5/11/11.5/12/12.5/13.5…） | ⚠️ スケール不在。0.5px刻みの中途半端 |
| 角丸 | **7種**（6/11/12/13/14/16/18px） | ⚠️ 統一されていない |
| padding | **13種**、うち9種が4の倍数外 | ⚠️ 8ptグリッド不在 |
| 書体 | 2族（Noto Sans JP / Zen Maru Gothic） | ✅ 適正 |

**構造的な罪 上位3つ**: ①色の過剰 ②タイポスケール不在 ③余白・角丸の不揃い
→ いずれも「足りない」ではなく**過剰**。修理は「揃える・減らす」であって「足す」ではない。

---

- **ジョブ**: 怪しい文面が届いた10〜30代が、3秒で危険度を確かめ、怖くなりすぎず相談まで進める
- **レジスター**: 主=**親愛**（安心して相談できる場所）／副=**劇場**（判定結果の開示の一瞬だけ）
  - 理由: 扱う題材は怖いが、UIまで怖いと「見なかったことに」される。心拍数は**保つ**が既定で、判定結果の開示でだけ上げる
- **サーフェス**: app（モバイル縦・単一HTML）

### パレット（実HEX・役割つき）

地の色はダークを維持する。製品名「ヤミバレ＝闇がバレる」と懐中電灯のメタファーが世界観の核であり、明るい地色にすると製品固有性を失うため。ただし**冷たい夜ではなく、灯りのある夜**に寄せる（青みを落とし、わずかに暖色へ）。

| 役割 | HEX | 用途 |
|---|---|---|
| 地 | `#0B0F1E` | 画面の背景 |
| 面 | `#161C33` | カード・ヘッダー・入力欄 |
| 面2 | `#1F2742` | カード内カード・吹き出し（AI側） |
| 境界 | `#2E3757` | 1pxの区切り線・枠 |
| 文字 | `#EDF0FA` | 本文 |
| 副文字 | `#98A1C4` | 補足・出典・プレースホルダ |
| **灯り（アクセント）** | `#FFD54D` | CTA・見出し・フォーカス・シグネチャーの光 |
| 自分の発言 | `#06C755` | ユーザー吹き出しのみ（LINEの文法を借りる） |

セマンティック（判定レベル4段）— **この4色以外で危険度を語らない**

| レベル | HEX | ラベル |
|---|---|---|
| lv3 | `#FF4D5E` | ソレヤバ!! |
| lv2 | `#FF9F1C` | かなりアヤシイ |
| lv1 | `#FFD54D` | チュウイ |
| lv0 | `#4DC9FF` | 判定材料すくなめ |

**合計11色**（30色から19色削減）。パレット外の色を足すときは、この表に追記してから足す。


### 補助色（役割が固定された派生色・上の11色に加えて使用可）

| HEX | 役割 |
|---|---|
| `#060911` | アプリ枠外の余白（520px超の画面での左右） |
| `#0A0E1B` | 沈み面（入力欄・メーターの溝・4コマの地） |
| `#20180A` | 灯り色ボタンの上の文字 |
| `#04240F` | 自分の吹き出しの上の文字 |
| `#FFFFFF` | 危険色ボタンの上の文字 |
| `#F4F6FF` / `#E7EAF7` / `#141A2B` | 4コマの吹き出し（発話=白／心の声=灰）とその文字。**暗い画面の中で紙のように光る**ことで「読み物」であることを示す意図的な例外 |

使用色は **30色 → 19色**（宣言11＋補助8）。

### タイポ

- display = **Zen Maru Gothic 900**（丸みのある表情。親愛レジスターの担い手）
- body = **Noto Sans JP 400/500/700**
- スケール = **8段の整数のみ**。0.5px刻みは禁止

`11 / 12 / 13 / 15 / 18 / 22 / 30 / 44`
（caption / small / body-s / body / title-s / title / display / hero）

### 余白リズム

- **8ptグリッド**。使う値は `4 / 8 / 12 / 16 / 20 / 24` の6種のみ（4は光学調整用の半グリッド）
- 角丸は **3種のみ**: `8px`（chip・badge）/ `14px`（card・bubble・button）/ `20px`（sheet）

### モーション

- 基準 **160ms ease-out**（状態変化の説明）
- 判定結果の開示のみ **420ms**（副レジスター＝劇場の唯一の出番）
- `prefers-reduced-motion` で光の走査とメーターのアニメを停止し、最終状態を即表示する

### シグネチャー: 「照らす」

判定結果カードが現れる瞬間、**画面上部から懐中電灯の光が斜めに差し込み、カードを照らし出す**（420ms・1回のみ）。ヤバ度メーターはその光がそのまま伸びた帯として描く。

製品名・アイコン（🔦）・「闇は、光を当てればバレる」というコピーと直結し、他アプリに流用できない。**祝祭ではなく開示の演出**なので、扱う題材と衝突しない。

### 禁じ手（このレジスター×題材で出やすいスロップ）

1. **恐怖の過剰演出** — 赤の点滅・警告音・シェイク。怖がらせると相談から遠のく。危険は色とコピーで示し、身振りでは示さない
2. **祝祭の流用** — 紙吹雪・スコアのカウントアップ祝い。詐欺判定は「達成」ではない
3. **色の再増殖** — 「ここだけ別の色」の積み重ねが30色を生んだ。判定4色＋灯り以外を使わない
4. **絵文字のサイズ不揃い** — 絵文字は文字ではなく図。サイズと行位置を明示的に揃える
